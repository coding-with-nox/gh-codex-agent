version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gh-codex-agent
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    ports:
      - "3000:3000"
    command: ["bun", "run", "src/main.ts"]